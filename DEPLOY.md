# 🚀 Інструкція для деплою на Vercel

## Крок 1: Підготуйте репозиторій

1. **Перевірте статус git:**
```bash
git status
```

2. **Якщо є незакомічені зміни, додайте їх:**
```bash
git add .
git commit -m "Додано множинне завантаження зображень для галереї та виправлено кольори тексту"
```

3. **Відправте зміни на GitHub:**
```bash
git push origin main
```

## Крок 2: Підключіть проект до Vercel

### Варіант A: Через веб-інтерфейс Vercel (рекомендовано)

1. Перейдіть на [vercel.com](https://vercel.com)
2. Увійдіть або зареєструйтеся (використовуйте GitHub аккаунт)
3. Натисніть **"Add New Project"** (або **"Import Project"**)
4. Виберіть ваш репозиторій `detailing4k.com.ua` з GitHub
5. Vercel автоматично визначить Next.js проект

### Варіант B: Через Vercel CLI

1. **Встановіть Vercel CLI** (якщо ще не встановлено):
```bash
npm i -g vercel
```

2. **Увійдіть в Vercel:**
```bash
vercel login
```

3. **Деплой проекту:**
```bash
vercel
```

Під час першого деплою Vercel запитає:
- Set up and deploy? **Y**
- Which scope? (виберіть свій аккаунт)
- Link to existing project? **N**
- What's your project's name? (можна залишити за замовчуванням)
- In which directory is your code located? **./** (поточна директорія)

## Крок 3: Налаштуйте змінні оточення (Environment Variables)

Після створення проекту на Vercel:

1. Перейдіть до **Settings** → **Environment Variables**
2. Додайте наступні змінні:

### Обов'язкові змінні:

```
DATABASE_URL=postgresql://user:password@host:port/database?schema=public
```

**Важливо:** Потрібна зовнішня PostgreSQL база даних (не локальна).

Рекомендовані хостинги PostgreSQL:
- **Vercel Postgres** (інтеграція з Vercel)
- **Neon** (neon.tech) - безкоштовний танець
- **Supabase** (supabase.com) - безкоштовний танець
- **Railway** (railway.app)
- **ElephantSQL** (elephantsql.com)

```
JWT_SECRET=your-very-long-and-random-secret-key-change-this
```

Генеруйте надійний секрет:
```bash
openssl rand -base64 32
```

### Опціональні змінні:

```
NEXTAUTH_SECRET=your-nextauth-secret-key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key (якщо використовується)
```

## Крок 4: Налаштуйте Build Settings

У **Settings** → **General** → **Build & Development Settings**:

1. **Framework Preset:** Next.js
2. **Build Command:** `npm run build` (за замовчуванням)
3. **Output Directory:** `.next` (за замовчуванням)
4. **Install Command:** `npm install` (за замовчуванням)

## Крок 5: Налаштуйте Prisma для Vercel

Додайте в `package.json` build script для Prisma:

```json
{
  "scripts": {
    "build": "prisma generate && next build",
    "postinstall": "prisma generate"
  }
}
```

Або створіть файл `vercel.json` в корені проекту:

```json
{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install"
}
```

## Крок 6: Налаштуйте базу даних на продакшн

### Якщо використовуєте Vercel Postgres:

1. У Vercel Dashboard → ваш проект → **Storage**
2. Натисніть **Create Database** → **Postgres**
3. Після створення скопіюйте `POSTGRES_URL` в Environment Variables як `DATABASE_URL`

### Якщо використовуєте зовнішню базу даних:

1. Створіть базу даних на вибраному хостингу
2. Скопіюйте connection string
3. Додайте як `DATABASE_URL` в Environment Variables

### Застосуйте міграції:

Після налаштування бази даних, застосуйте міграції:

**Варіант 1: Через Vercel CLI**
```bash
vercel env pull .env.local
npx prisma migrate deploy
```

**Варіант 2: Через SSH/термінал хостингу БД**
```bash
DATABASE_URL="your-production-database-url" npx prisma migrate deploy
```

**Варіант 3: Через Prisma Studio (для тестування)**
```bash
DATABASE_URL="your-production-database-url" npx prisma studio
```

## Крок 7: Запустіть деплой

### Автоматичний деплой (через GitHub):

Після налаштування, кожен push в `main` гілку автоматично запускає деплой.

### Ручний деплой (через CLI):

```bash
vercel --prod
```

## Крок 8: Перевірте деплой

1. Після успішного деплою отримаєте URL (наприклад: `your-project.vercel.app`)
2. Перевірте, що сайт працює
3. Перевірте API endpoints
4. Перевірте підключення до бази даних

## 🔧 Рішення проблем

### Помилка з Prisma Client:

Якщо отримуєте помилку `Prisma Client not generated`, додайте в `package.json`:

```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

### Помилка з базою даних:

1. Перевірте, що `DATABASE_URL` правильно налаштований
2. Перевірте, що база даних доступна з інтернету (не localhost)
3. Перевірте firewall правила хостингу БД

### Помилки збірки:

1. Перевірте логи в Vercel Dashboard → ваш проект → **Deployments** → вибрати deployment → **Build Logs**
2. Переконайтеся, що всі залежності вказані в `package.json`
3. Перевірте, що TypeScript помилки виправлені

## 📝 Важливі нотатки

1. **Файли завантаження:** Файли з `/public/uploads` не зберігаються між деплоями. Рекомендується використовувати:
   - Vercel Blob Storage
   - AWS S3
   - Cloudinary
   - Інші хмарні сервіси для файлів

2. **Environment Variables:** Зміни в Environment Variables вимагають нового деплою

3. **База даних:** Локальна база даних (Docker) не працюватиме на Vercel. Потрібна зовнішня БД

## 🎉 Готово!

Після успішного деплою ваш сайт буде доступний за адресою `your-project.vercel.app`

Для кастомного домену:
1. Vercel Dashboard → ваш проект → **Settings** → **Domains**
2. Додайте свій домен та налаштуйте DNS записи

