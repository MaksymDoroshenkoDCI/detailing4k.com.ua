# 🔧 Рішення проблем після деплою

## Проблема: Немає послуг, галереї та не можна залогінитись

Це зазвичай означає, що база даних не налаштована або не підключена на Vercel.

### Крок 1: Перевірте Environment Variables на Vercel

1. Перейдіть до Vercel Dashboard → ваш проект → **Settings** → **Environment Variables**
2. Переконайтеся, що налаштовано:

#### Обов'язкові змінні:

```
DATABASE_URL=postgresql://user:password@host:port/database?schema=public
JWT_SECRET=your-very-long-and-random-secret-key
```

### Крок 2: Створіть базу даних (якщо ще не створено)

#### Варіант A: Vercel Postgres (рекомендовано)

1. Vercel Dashboard → ваш проект → **Storage**
2. Натисніть **Create Database** → **Postgres**
3. Після створення скопіюйте `POSTGRES_URL` 
4. Додайте як `DATABASE_URL` в Environment Variables
5. Перезапустіть деплой (Settings → Redeploy)

#### Варіант B: Neon (безкоштовний)

1. Перейдіть на [neon.tech](https://neon.tech)
2. Створіть безкоштовний акаунт
3. Створіть новий проект
4. Скопіюйте connection string (виглядає як: `postgresql://user:password@host.neon.tech/dbname`)
5. Додайте як `DATABASE_URL` в Vercel Environment Variables
6. Перезапустіть деплой

#### Варіант C: Supabase (безкоштовний)

1. Перейдіть на [supabase.com](https://supabase.com)
2. Створіть безкоштовний акаунт
3. Створіть новий проект
4. Перейдіть до **Settings** → **Database**
5. Скопіюйте connection string з розділу "Connection string" → "URI"
6. Додайте як `DATABASE_URL` в Vercel Environment Variables
7. Перезапустіть деплой

### Крок 3: Застосуйте міграції бази даних

Після налаштування `DATABASE_URL`, потрібно застосувати міграції Prisma:

#### Варіант 1: Через Vercel CLI (рекомендовано)

1. Встановіть Vercel CLI (якщо ще не встановлено):
```bash
npm i -g vercel
```

2. Увійдіть в Vercel:
```bash
vercel login
```

3. Перейдіть до директорії проекту та завантажте environment variables:
```bash
cd /path/to/your/project
vercel env pull .env.local
```

4. Застосуйте міграції:
```bash
npx prisma migrate deploy
```

Або через DATABASE_URL безпосередньо:
```bash
DATABASE_URL="your-production-database-url" npx prisma migrate deploy
```

#### Варіант 2: Через SQL (якщо CLI не працює)

1. Підключіться до вашої бази даних через SQL клієнт (pgAdmin, DBeaver, або веб-інтерфейс хостингу)
2. Перейдіть до файлу `prisma/migrations/20251229173347_init/migration.sql`
3. Скопіюйте весь SQL код з файлу
4. Виконайте SQL код в базі даних

### Крок 4: Перевірте підключення до бази даних

1. Перейдіть до Vercel Dashboard → ваш проект → **Deployments**
2. Відкрийте останній деплой → **Functions**
3. Перевірте логи на наявність помилок підключення до БД

### Крок 5: Створіть адміністратора

Після застосування міграцій, потрібно створити адміністратора:

#### Варіант 1: Через SQL (швидко)

Підключіться до бази даних та виконайте:

```sql
INSERT INTO admins (admin_id, name, email, password_hash, role, created_at, updated_at)
VALUES (
  gen_random_uuid(),
  'Admin User',
  'admin@example.com',
  '$2a$10$YourHashedPasswordHere',
  'SuperAdmin',
  NOW(),
  NOW()
);
```

**Важливо:** `password_hash` повинен бути захешованим паролем. Використовуйте скрипт нижче для генерації.

#### Варіант 2: Через скрипт

Створіть тимчасовий скрипт для створення адміністратора:

1. Створіть файл `scripts/create-admin-production.js`:
```javascript
const { PrismaClient } = require('@prisma/client')
const bcrypt = require('bcryptjs')

const prisma = new PrismaClient({
  datasources: {
    db: {
      url: process.env.DATABASE_URL
    }
  }
})

async function createAdmin() {
  try {
    const email = 'admin@example.com'
    const password = 'admin123' // Змініть на безпечний пароль!
    const name = 'Admin User'

    const passwordHash = await bcrypt.hash(password, 10)

    const existing = await prisma.admin.findUnique({
      where: { email },
    })

    if (existing) {
      console.log('Admin already exists:', email)
      return
    }

    const admin = await prisma.admin.create({
      data: {
        name,
        email,
        passwordHash,
        role: 'SuperAdmin',
      },
    })

    console.log('✅ Admin created successfully!')
    console.log('Email:', admin.email)
    console.log('Password:', password)
    console.log('Role:', admin.role)
  } catch (error) {
    console.error('Error creating admin:', error)
  } finally {
    await prisma.$disconnect()
  }
}

createAdmin()
```

2. Виконайте скрипт з production DATABASE_URL:
```bash
DATABASE_URL="your-production-database-url" node scripts/create-admin-production.js
```

### Крок 6: Додайте тестові дані (опціонально)

Після створення адміністратора, ви можете:
1. Увійти через адмін панель на `/admin`
2. Додати послуги через "Керування послугами"
3. Додати зображення в галерею через "Керування галереєю"

### Крок 7: Перезапустіть деплой

Після всіх змін:

1. Vercel Dashboard → ваш проект → **Deployments**
2. Натисніть **⋯** (три крапки) на останньому деплої
3. Виберіть **Redeploy**

Або через CLI:
```bash
vercel --prod
```

## Перевірка роботи

1. Відкрийте ваш сайт на Vercel
2. Спробуйте увійти: `/login` (використовуйте email та password адміністратора)
3. Перевірте послуги: `/services`
4. Перевірте галерею: `/gallery`

## Часті помилки

### "Prisma Client not generated"
- Переконайтеся, що `postinstall` script в `package.json` включає `prisma generate`
- Переконайтеся, що `build` script включає `prisma generate && next build`

### "Can't reach database server"
- Перевірте, що `DATABASE_URL` правильний
- Перевірте, що база даних доступна з інтернету (не localhost)
- Перевірте firewall правила хостингу БД

### "relation does not exist"
- Міграції не застосовані - виконайте `prisma migrate deploy`

### "password authentication failed"
- Перевірте username та password в `DATABASE_URL`
- Переконайтеся, що використовуєте правильні credentials

## Додаткова допомога

Якщо проблема не вирішена:
1. Перевірте логи в Vercel Dashboard → ваш проект → **Deployments** → **Functions** → виберіть функцію → **Logs**
2. Перевірте, що всі environment variables правильно налаштовані
3. Переконайтеся, що база даних активна та доступна

